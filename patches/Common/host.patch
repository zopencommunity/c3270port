diff --git a/Common/host.c b/Common/host.c
index 0510d38..eff0f49 100644
--- a/Common/host.c
+++ b/Common/host.c
@@ -687,6 +687,13 @@ host_connect(const char *n, enum iaction ia)
     if (ps == NULL) {
 	ps = appres.login_macro;
     }
+
+    /* Do not run the login macro if Express Logon is active. */
+    if (appres.express_logon) {
+        ps = NULL;
+        vtrace("Express Logon active: suppressing login macro.\n");
+    }
+
     if (ps != NULL) {
 	login_macro(ps);
     }

# Made with Bob
