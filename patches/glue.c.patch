diff --git a/Common/glue.c b/Common/glue.c
index e445b3b..89858e3 100644
--- a/Common/glue.c
+++ b/Common/glue.c
@@ -528,24 +528,22 @@ validate_elf_applid(const char *applid)
 
 /*
  * Send an ELF token to the host.
- * Converts the token to UCS-4, sends it, and presses Enter.
+ * Sends the token string followed by Enter.
  */
 void
 send_elf_token(const char *token)
 {
+    char *token_with_enter;
     size_t len = strlen(token);
-    ucs4_t *ucs_token = (ucs4_t *)Malloc(len * sizeof(ucs4_t));
-    size_t i;
 
-    /* Convert ASCII token to UCS-4 */
-    for (i = 0; i < len; i++) {
-	ucs_token[i] = (ucs4_t)token[i];
-    }
+    /* Allocate space for token + newline + null terminator */
+    token_with_enter = (char *)Malloc(len + 2);
+    strcpy(token_with_enter, token);
+    strcat(token_with_enter, "\n");
 
-    /* Send token and press Enter */
-    emulate_uinput(ucs_token, len, false);
-    Free(ucs_token);
-    key_AID(AID_ENTER);
+    /* Send token with Enter (newline triggers Enter) */
+    emulate_input(token_with_enter, len + 1, false, false);
+    Free(token_with_enter);
 }
 
 /*
