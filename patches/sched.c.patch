diff --git a/Common/sched.c b/Common/sched.c
index a5a0bc6..75d7794 100644
--- a/Common/sched.c
+++ b/Common/sched.c
@@ -810,6 +810,7 @@ process_some_events(bool block, bool *processed_any)
     /* Process the events that completed. */
     inputs_changed = false;
     i = 0;
+    (void)i; /* May be unused depending on platform-specific macros */
     for (ip = inputs; ip != NULL; ip = ip->next) {
 	if (ip->valid) {
 	    if (!(ip->sflags & SF_CURRENT)) {
